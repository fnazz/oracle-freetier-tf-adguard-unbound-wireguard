#!/bin/bash -x 
HOST=$(hostname -f)
DATE=$(date +"%d%m%Y-%H%M%S")
LOGFILE="/tmp/pihole-init_${HOST}_${DATE}.log"

function os_actions() {
    # Prereqs and docker
    echo "I do nothing for now"
    
}

# os_actions


